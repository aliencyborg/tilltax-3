<div class="flex w-full h-full">
  {{#if this.showHide.showSplash}}
    {{outlet}}
  {{else}}
    <TtSidebar />

    <div class="flex flex-grow">
      <div class="pg-purple-lightest flex-1">
        {{outlet}}
      </div>
    </div>
  {{/if}}
</div>
